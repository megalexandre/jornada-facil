# frozen_string_literal: true

# Suíte BDD (Cucumber) do monorepo. Roda caixa-preta sobre a API por HTTP —
# não é um app Rails, só o toolchain de teste. Ver features/ e README.md.
source "https://rubygems.org"

gem "cucumber", "~> 9.2"
gem "httparty", "~> 0.22"        # cliente HTTP fino
gem "rspec-expectations", "~> 3.13" # matchers: expect(...).to eq(...)
gem "rake", "~> 13.0"
