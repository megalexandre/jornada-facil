name: jornada

services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: ${DATABASE_USER:-postgres}
      POSTGRES_PASSWORD: ${API_DATABASE_PASSWORD:-postgres}
      POSTGRES_DB: ${DATABASE_NAME:-api_development}
    ports:
      # lado do host configurável; dentro da rede o Postgres escuta sempre em 5432
      - "${DATABASE_PORT:-5432}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    # O servidor Rails NÃO sobe aqui: o container só prepara o banco e fica vivo
    # (sleep infinity). O dev inicia o server pela task "Rails: server" do VS Code
    # (ou `make rails`), com os logs num terminal próprio. Ver AGENTS.md.
    command: >
      bash -lc "(bundle check || bundle install) &&
      bin/rails db:migrate &&
      bin/rails db:seed &&
      sleep infinity"
    working_dir: /workspaces/jornada/api
    volumes:
      - .:/workspaces/jornada:cached   # raiz inteira (o devcontainer edita tudo)
      - bundle:/usr/local/bundle       # gems isoladas do host
      - pub_cache:/root/.pub-cache     # cache de pacotes Flutter (o app roda neste container, via F5)
    environment:
      RAILS_ENV: ${RAILS_ENV:-development}
      DATABASE_HOST: ${DATABASE_HOST:-db}
      # porta interna do Postgres na rede do compose (não a publicada no host)
      DATABASE_PORT: "5432"
      DATABASE_USER: ${DATABASE_USER:-postgres}
      API_DATABASE_PASSWORD: ${API_DATABASE_PASSWORD:-postgres}
      DATABASE_NAME: ${DATABASE_NAME:-api_development}
    ports:
      - "${RAILS_PORT:-3000}:${RAILS_PORT:-3000}"
    depends_on:
      db:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Suíte BDD da raiz (Cucumber em Ruby, caixa-preta sobre a API por HTTP).
  # Sobe só sob demanda (profile `bdd`); dentro da rede a API é `http://api:3000`.
  # ---------------------------------------------------------------------------
  tests:
    image: ruby:3.4.10-slim
    profiles: ["bdd"]
    command: >
      bash -lc "apt-get update -qq &&
      apt-get install -y --no-install-recommends build-essential git >/dev/null &&
      (bundle check || bundle install) &&
      bundle exec cucumber"
    working_dir: /workspaces/jornada
    volumes:
      - .:/workspaces/jornada:cached
      - bdd_bundle:/usr/local/bundle
    environment:
      API_BASE_URL: ${API_BASE_URL:-http://api:3000}
      API_HOST_HEADER: ${API_HOST_HEADER:-localhost}   # Rails 8 dev só aceita localhost/IP no Host
    depends_on:
      - api

volumes:
  pg_data:
  bundle:
  pub_cache:
  bdd_bundle:
